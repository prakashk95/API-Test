Test cases for User Creation:
pm.test("Status code is 201", function () {
    pm.response.to.have.status(200)
});
pm.test("Response body contains 'User created!'", function () {
pm.expect(pm.response.json().message).to.equal('User created!')})

Request Params to create users: name, email, password, title (for example: Mr, Mrs, Miss), birth_date, birth_month, birth_year, 
firstname, lastname, company, address1, address2, country, zipcode, state, city, mobile_number
